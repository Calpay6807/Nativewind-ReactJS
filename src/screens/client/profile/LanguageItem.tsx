import {StyleSheet, Text, View} from 'react-native';
import React from 'react';
import {Items} from './items';

const LanguageItem = () => {
  return (
    <>
      <Items text={'Language'} value={'Tr'} onPress={() => {}} />
    </>
  );
};

export default LanguageItem;

const styles = StyleSheet.create({});
